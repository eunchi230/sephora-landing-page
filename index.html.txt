<index.html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sephora Gift Card</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Didact+Gothic&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #0a0a0a;
    font-family: 'Didact Gothic', sans-serif;
    padding: 24px;
    background-image: 
      radial-gradient(ellipse at 20% 50%, rgba(180,120,100,0.08) 0%, transparent 60%),
      radial-gradient(ellipse at 80% 20%, rgba(200,150,120,0.06) 0%, transparent 50%);
  }

  .notification {
    position: fixed;
    top: 20px;
    left: 50%;
    transform: translateX(-50%);
    background: #1a1a1a;
    border: 1px solid #333;
    color: #e8d5c4;
    padding: 10px 20px;
    border-radius: 50px;
    font-size: 13px;
    letter-spacing: 0.04em;
    display: flex;
    align-items: center;
    gap: 8px;
    animation: slideDown 0.6s ease forwards;
    white-space: nowrap;
  }

  .notification span.dot {
    width: 6px; height: 6px;
    background: #c9a880;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:0.4} }
  @keyframes slideDown { from{opacity:0;top:0} to{opacity:1;top:20px} }

  .card {
    width: 100%;
    max-width: 400px;
    background: #111;
    border: 1px solid #222;
    border-radius: 24px;
    overflow: hidden;
    animation: fadeUp 0.8s ease 0.2s both;
  }

  @keyframes fadeUp { from{opacity:0;transform:translateY(20px)} to{opacity:1;transform:translateY(0)} }

  .card-header {
    background: linear-gradient(135deg, #1a1a1a 0%, #0f0f0f 100%);
    padding: 40px 32px 32px;
    text-align: center;
    border-bottom: 1px solid #1f1f1f;
    position: relative;
    overflow: hidden;
  }

  .card-header::before {
    content: '';
    position: absolute;
    top: -60px; left: -60px; right: -60px;
    height: 120px;
    background: radial-gradient(ellipse, rgba(180,130,100,0.12) 0%, transparent 70%);
  }

  .logo {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    letter-spacing: 0.15em;
    color: #fff;
    margin-bottom: 4px;
    position: relative;
  }

  .logo-sub {
    font-size: 10px;
    letter-spacing: 0.4em;
    color: #666;
    text-transform: uppercase;
    margin-bottom: 28px;
  }

  .amount {
    font-family: 'Playfair Display', serif;
    font-size: 72px;
    color: #fff;
    line-height: 1;
    margin-bottom: 6px;
  }

  .amount-label {
    font-size: 11px;
    letter-spacing: 0.35em;
    color: #888;
    text-transform: uppercase;
  }

  .timer-bar {
    margin: 28px 32px 0;
    background: #1a1a1a;
    border: 1px solid #2a2a2a;
    border-radius: 10px;
    padding: 12px 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    color: #c9a880;
    font-size: 13px;
    letter-spacing: 0.06em;
  }

  .timer-icon {
    width: 14px; height: 14px;
    border: 1.5px solid #c9a880;
    border-radius: 50%;
    position: relative;
    flex-shrink: 0;
  }

  .timer-icon::after {
    content: '';
    position: absolute;
    top: 2px; left: 6px;
    width: 1.5px; height: 5px;
    background: #c9a880;
    transform-origin: bottom center;
  }

  .steps {
    padding: 24px 32px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .step {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 14px 16px;
    background: #161616;
    border: 1px solid #1f1f1f;
    border-radius: 12px;
    animation: fadeUp 0.6s ease both;
  }

  .step:nth-child(1) { animation-delay: 0.4s; }
  .step:nth-child(2) { animation-delay: 0.5s; }
  .step:nth-child(3) { animation-delay: 0.6s; }
  .step:nth-child(4) { animation-delay: 0.7s; }

  .step-num {
    width: 30px; height: 30px;
    background: linear-gradient(135deg, #c9a880, #a07850);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 12px;
    font-weight: bold;
    color: #000;
    flex-shrink: 0;
  }

  .step-text {
    font-size: 14px;
    color: #ccc;
    letter-spacing: 0.02em;
  }

  .cta-wrap {
    padding: 8px 32px 32px;
  }

  .cta-btn {
    width: 100%;
    background: linear-gradient(135deg, #c9a880 0%, #a07850 100%);
    border: none;
    border-radius: 14px;
    padding: 18px;
    font-family: 'Didact Gothic', sans-serif;
    font-size: 13px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #000;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .cta-btn::after {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(255,255,255,0);
    transition: background 0.3s;
  }

  .cta-btn:hover::after { background: rgba(255,255,255,0.1); }
  .cta-btn:hover { transform: translateY(-1px); box-shadow: 0 8px 24px rgba(180,130,80,0.3); }
  .cta-btn:active { transform: translateY(0); }

  .arrow {
    font-size: 16px;
    transition: transform 0.3s;
  }
  .cta-btn:hover .arrow { transform: translateX(4px); }
</style>
</head>
<body>

<div class="notification">
  <span class="dot"></span>
  Priya M. just claimed $450
</div>

<div class="card">
  <div class="card-header">
    <div class="logo">SEPHORA</div>
    <div class="logo-sub">Beauty & Beyond</div>
    <div class="amount">$750</div>
    <div class="amount-label">Sephora Gift Card</div>
  </div>

  <div class="timer-bar">
    <div class="timer-icon"></div>
    Offer expires in <strong id="timer">14:47</strong>
  </div>

  <div class="steps">
    <div class="step">
      <div class="step-num">1</div>
      <div class="step-text">Click The Button Below</div>
    </div>
    <div class="step">
      <div class="step-num">2</div>
      <div class="step-text">Enter Your Basic Info</div>
    </div>
    <div class="step">
      <div class="step-num">3</div>
      <div class="step-text">Complete 3–5 Deals</div>
    </div>
    <div class="step">
      <div class="step-num">4</div>
      <div class="step-text">Claim Your Reward</div>
    </div>
  </div>

  <div class="cta-wrap">
    <button class="cta-btn">
      Claim Your Gift Card <span class="arrow">→</span>
    </button>
  </div>
</div>

<script>
  let seconds = 14 * 60 + 47;
  const timerEl = document.getElementById('timer');
  setInterval(() => {
    if (seconds > 0) seconds--;
    const m = String(Math.floor(seconds / 60)).padStart(2, '0');
    const s = String(seconds % 60).padStart(2, '0');
    timerEl.textContent = `${m}:${s}`;
  }, 1000);
</script>
</body>
</html>